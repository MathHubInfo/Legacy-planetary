<?php
/**
 * @file
 */

// Drupal needs this (not quite) blank file.

function planetmath_menus_menu_alter(&$items) {

  $items['poll']['access callback'] = FALSE;
  $items['aggregator']['hidden'] = TRUE;
  $items['sysstats']['hidden'] = TRUE;
}