<?php


function planetary_authorlinks_theme($existing, $type, $theme, $path) {
  return array(
      'planetary_links' => array(
          'variables' => array('links' => array()),
      ),
  );
}

function theme_planetary_links($variables) {
  return implode(' | ', $variables['links']);
}